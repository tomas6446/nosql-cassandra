import express from 'express';
const router = express.Router();

import {
  // addCar,
  // getCar,
  // deleteCar,
  // getCars,
  // getCarRents,
} from "../controllers/cars.js";

// router.get("/", getCars);
// router.post("/", addCar);
// router.get("/:id", getCar);
// router.delete("/:id", deleteCar);
// router.get("/:id/reservations", getCarRents);

export default router;