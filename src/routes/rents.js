import express from 'express';
const router = express.Router();

import {
//   addCarRent,
//   removeCarRent,
} from "../controllers/rents.js";

// router.post("/", addCarRent);
// router.delete("/:id", removeCarRent);

export default router;